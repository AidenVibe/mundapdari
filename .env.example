# ========================================
# 🔒 환경변수 보안 가이드
# ========================================
# 
# ⚠️  중요: 이 파일은 예시용입니다. 실제 값은 .env 파일에 저장하세요!
#
# 🛡️  보안 규칙:
# 1. .env 파일은 절대 Git에 커밋하지 마세요
# 2. 모든 시크릿과 API 키는 강력한 랜덤 값을 사용하세요
# 3. 운영환경과 개발환경의 키는 다르게 사용하세요
# 4. 주기적으로 키를 교체하세요
# 5. 팀원과 키를 공유할 때는 안전한 방법을 사용하세요
#
# 🔑 키 생성 도구:
# - JWT/암호화 키: openssl rand -hex 32
# - 패스워드: openssl rand -base64 32
# - UUID: uuidgen (macOS/Linux) 또는 온라인 생성기
#
# ========================================

# Application Configuration
NODE_ENV=development
PORT=3000
API_BASE_URL=http://localhost:3000

# Database Configuration (PostgreSQL for production)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mundapdari
DB_USER=postgres
DB_PASSWORD=password
DB_SSL=false

# SQLite Configuration (for development)
SQLITE_PATH=./database/mundapdari.db

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT Configuration
# 🔒 보안: 실제 운영환경에서는 강력한 랜덤 키를 사용하세요 (최소 32자)
# 생성 예: openssl rand -hex 32
JWT_SECRET=CHANGE_THIS_TO_STRONG_RANDOM_32_CHAR_KEY_IN_PRODUCTION
JWT_EXPIRES_IN=24h
JWT_REFRESH_SECRET=CHANGE_THIS_TO_DIFFERENT_STRONG_RANDOM_32_CHAR_KEY
JWT_REFRESH_EXPIRES_IN=7d

# Encryption Configuration
# 🔒 보안: 전화번호 암호화용 키 (정확히 32자 hex)
# 생성 예: openssl rand -hex 16
ENCRYPTION_KEY=CHANGE_THIS_TO_32_CHAR_HEX_KEY_FOR_ENCRYPTION

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS Configuration
CORS_ORIGIN=http://localhost:3000,https://pf.kakao.com

# External Services
KAKAO_API_URL=https://kapi.kakao.com
# 🔒 보안: 카카오 개발자 콘솔에서 발급받은 실제 키를 사용하세요
KAKAO_APP_KEY=YOUR_KAKAO_APP_KEY_FROM_DEVELOPER_CONSOLE
KAKAO_APP_SECRET=YOUR_KAKAO_APP_SECRET_FROM_DEVELOPER_CONSOLE

# AWS Configuration (for production)
AWS_REGION=ap-northeast-2
# 🔒 보안: AWS IAM에서 최소 권한 원칙에 따라 생성된 키를 사용하세요
AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_ACCESS_KEY
AWS_S3_BUCKET=mundapdari-images

# Monitoring
# 🔒 보안: Sentry 프로젝트에서 발급받은 실제 DSN을 사용하세요
SENTRY_DSN=YOUR_SENTRY_DSN_URL_FROM_PROJECT_SETTINGS
LOG_LEVEL=info

# Notification Settings
NOTIFICATION_QUEUE_REDIS_URL=redis://localhost:6379
MESSAGE_RETRY_ATTEMPTS=3
MESSAGE_RETRY_DELAY=5000

# Development Settings
ENABLE_SWAGGER=true
ENABLE_CORS_DEBUG=true
ENABLE_SQL_LOGGING=false